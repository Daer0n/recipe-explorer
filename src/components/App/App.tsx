import React from 'react';
import { Router } from '@components/Router';

import { Container } from './styled';

export const App = () => {
    return (
        <Container>
            <Router />
        </Container>
    );
};
